{
  "name": "todo_react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install",
    "start": "npm run babel && npm run server",
    "server": "http-server dist -p 8080",
    "babel": "babel src --out-dir dist --copy-files",
    "babel-watch": "babel src --watch --out-dir dist --copy-files",
    "clean": "git clean -xfd"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monotkate/todo_react.git"
  },
  "author": "",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/monotkate/todo_react/issues"
  },
  "homepage": "https://github.com/monotkate/todo_react/#readme",
  "dependencies": {
    "babel-cli": "^6.6.4",
    "babel-core": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "http-server": "^0.9.0"
  },
  "babel": {
    "presets": ["react"],
    "only": [
      "*.jsx"
    ]
  }
}
